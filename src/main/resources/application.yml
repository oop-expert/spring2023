
server:
  port: 8080
spring:
  profiles:
    active: test
  datasource:
    url: jdbc:mysql://localhost:3306/spring_app
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  freemarker:
    expose-request-attributes: true